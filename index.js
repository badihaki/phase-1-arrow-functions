// making funct expression
const divide = function(){
    // doesn't need parameters
    return 2000/100;
}

// arrow funct's
const square = inputNumber => inputNumber*inputNumber;
const add = (number1, number2) => number1 + number2;